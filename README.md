# test_registryScanner

Solution has been created in VS2015;

NOTE:

Steps to run component test :

1)right button click on unitTests project in registryScanner.sln -> Set as Startup Project
2)right button click on registryScanner project in registryScanner.sln -> Properties -> General -> Configuration type   and choose Static library (.lib) instead of Application (.exe) 
